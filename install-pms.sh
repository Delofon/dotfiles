#!/usr/bin/env bash

# Vundle was recommended by a tmux plugin but I've see many people use VimPlug instead so idk
# Even though my .vimrc is based around Vundle it should not be a big problem to adapt it
# over to VimPlug.
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim 
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

